TASK: Add optional NetworkPolicy template and optional HPA custom-metric values to Helm chart

COMPLETION EVIDENCE
===================

1. NetworkPolicy Template Created
   File: charts/image-optimize-proxy/templates/networkpolicy.yaml
   - Gated by networkPolicy.enabled flag (default: false)
   - Supports optional ingressFromCIDRs list
   - Supports optional egressToS3 and egressToDNS flags
   - Allows imgproxy sidecar traffic on port 8081
   - Allows DNS on ports 53 (UDP/TCP)
   - Allows HTTPS on port 443 when egressToS3 enabled

2. Values Updated
   File: charts/image-optimize-proxy/values.yaml
   - Added networkPolicy block with enabled, ingressFromCIDRs, egressToS3, egressToDNS
   - Added hpa.customMetrics array with example comment

3. HPA Template Enhanced
   File: charts/image-optimize-proxy/templates/hpa.yaml
   - Added support for hpa.customMetrics via toYaml
   - Preserves existing CPU/memory metrics
   - Custom metrics appended after standard metrics

4. Helm Template Verification
   ✓ NetworkPolicy renders when enabled=true with CIDR ingress rules
   ✓ NetworkPolicy does NOT render when enabled=false
   ✓ HPA renders with custom metrics when provided
   ✓ HPA renders without custom metrics when empty (backward compatible)
   ✓ All templates validate without errors

Test Commands Executed:
  helm template image-optimize-proxy ./charts/image-optimize-proxy \
    --set config.cacheS3Bucket=test-cache \
    --set config.allowAllUpstreamGateways=true \
    --set config.allowAllSourceBuckets=true \
    --set networkPolicy.enabled=true \
    --set 'networkPolicy.ingressFromCIDRs[0]=10.0.0.0/8'
  
  Result: NetworkPolicy rendered with ingress from 10.0.0.0/8

  helm template image-optimize-proxy ./charts/image-optimize-proxy \
    --set config.cacheS3Bucket=test-cache \
    --set networkPolicy.enabled=false
  
  Result: NetworkPolicy NOT rendered (0 matches)

  helm template image-optimize-proxy ./charts/image-optimize-proxy \
    --set config.cacheS3Bucket=test-cache \
    --set 'hpa.customMetrics[0].type=Pods' \
    --set 'hpa.customMetrics[0].pods.metric.name=http_request_duration_seconds_p95' \
    --set 'hpa.customMetrics[0].pods.target.type=AverageValue' \
    --set 'hpa.customMetrics[0].pods.target.averageValue=2'
  
  Result: HPA rendered with custom Pods metric for p95 latency

5. No Breaking Changes
   - Existing HPA CPU/memory metrics still work
   - NetworkPolicy disabled by default (opt-in)
   - No modifications to service, deployment, or serviceaccount templates
   - No modifications to NLB annotations

COMMIT MESSAGE
==============
feat(helm): add optional network and scaling hardening

- Add NetworkPolicy template gated by networkPolicy.enabled (default: false)
- Support optional ingress CIDR rules via networkPolicy.ingressFromCIDRs
- Support optional egress to S3 and DNS via networkPolicy.egressToS3/egressToDNS
- Add hpa.customMetrics array for Pods/External metrics (prometheus-adapter, KEDA)
- Preserve backward compatibility: existing HPA CPU/memory metrics unaffected
- All templates verified with helm template
